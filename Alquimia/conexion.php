<?php
$host = "localhost";
$usuario = "root";
$password = ""; 
$bd = "alquimia_db";

// Conexión simulada temporalmente para despliegue
$conn = null;

/*
$conn = new mysqli($host, $usuario, $password, $bd);

// Verificar conexión
if ($conn->connect_error) {
    die("Error de conexión: " . $conn->connect_error);
}
*/
?>
